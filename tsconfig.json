{
    "compilerOptions": {
        "module" : "CommonJS",
        "target" : "ES2015",
        "sourceMap" : true,
        "outDir" : "dist"
    },
    // 어떤 파일들이 컴파일 과정에 포함되는지 Typescript에게 알려줌
    // 컴파일 과정에서 포함할 파일의 배열을 적음
    "include" : ["src/**/*"],
    "exclude" : ["node_modules"]
}